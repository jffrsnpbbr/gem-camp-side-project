<h1>Home</h1>

<% if user_signed_in? %>
  <h2> Hello <%= current_user.email %> </h2>
<%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to logout?'} %>
<% else %>
  <%= link_to 'Sign in', new_user_session_path %>
<% end %>